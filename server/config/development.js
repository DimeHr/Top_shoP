'use strict';

module.exports = {
    mongodb: {
        description: 'localhost',
        uri: 'mongodb://localhost:27017/top-shop',
        options: {
            safe: true,
            auto_reconnect: true
        }
    },
}
